# Note: repo content is in English (portfolio for international roles).
# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtualenv
.venv/
venv/
env/

# Tests / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Local database (SQLite arrives in the next milestones)
*.db
*.sqlite3

# Secrets / local config (NEVER commit an LLM API key)
.env
.env.*
!.env.example

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Agent skills (dev tooling, not product). Reinstall with:
#   npx @tech-leads-club/agent-skills install --force --skill tlc-spec-driven
.claude/
.cursor/
.windsurf/
.agents/
